# 安全说明

请不要在 issue、pull request 或截图中公开 Microsoft 登录令牌、DeepSeek/API Key、任务内容或本机路径。

如果发现可能导致凭据泄露、跨来源写入或未授权访问的问题，请先通过 GitHub 的私密漏洞报告渠道联系维护者，并提供最小复现步骤。发现密钥已经泄露时，应立即在对应服务商处撤销并重新生成。

本项目默认只监听 `127.0.0.1`。如果你要把它部署到局域网或公网，必须自行增加认证、HTTPS、访问控制和密钥管理；当前仓库不把这种部署视为默认安全配置。
